function CategoryItemView({ it }) {
    return (
        <div className="cat-item-view">
            <h4>
                {it.name}
            </h4>
        </div>
    );
}

export default CategoryItemView;